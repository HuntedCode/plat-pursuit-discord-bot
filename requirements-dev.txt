# Development / test-only dependencies.
# NOT installed into the production Docker image (the Dockerfile copies only
# requirements.txt). Install locally with:  pip install -r requirements-dev.txt
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.24.0       # async test + fixture support (discord.py / SQLAlchemy are async)
aioresponses==0.7.6          # mock outbound aiohttp calls (the PlatPursuit API) at the boundary
